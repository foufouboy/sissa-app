// Implémenter en tenant compte du fait que nous n'avons encore rien défini pour les emails
// Juste les fonctions nécessaires au CRUD des notifs et au read de tous les messages
/**
 * MessageModel.findAll()
 * MessageModel.findAllOfType(type: string)
 * MessageModel.findById(id: string)
 * 
 * MessageModel.createNotification(data: CreateNotificationInput)
 * MessageModel.createMail(data: CreateMailInput)
 * 
 * MessageModel.delete(id: string)
 * 
 */